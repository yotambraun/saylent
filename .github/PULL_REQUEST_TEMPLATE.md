## What this changes and why

<!-- One or two sentences. What problem does this solve, or what does it add? -->

## Checklist

- [ ] `npm run typecheck` is clean
- [ ] `npm run lint` is clean
- [ ] `npm run test` is green
- [ ] `npm run build` succeeds
- [ ] Docs updated if this touches a documented command, flag, extension
      point, or environment variable (`ARCHITECTURE.md`, `METHODOLOGY.md`,
      or a `website/content/docs/*` page)

## Notes for the reviewer

<!-- Anything that needs a closer look: judge/sampling/fix-weight changes,
     a new dependency, a breaking change to the run-bundle format, etc.
     Leave blank if there's nothing unusual. -->
