// Moved into @saylent/report (see ARCHITECTURE.md). Wording unchanged; the
// correction address and the methodology link now come from the report host
// so an operator's deployment states its own.
export { ReportDisclaimer } from "@saylent/report/components/report-disclaimer";
