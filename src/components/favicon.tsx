"use client";
// Moved into @saylent/report (open-source split).
// The icon URL now comes from the report host — the app's default is unchanged
// (/api/favicon, same-origin proxy); a static report.html gets the letter tile.
export { Favicon, faviconTile, hostHue } from "@saylent/report/components/favicon";
