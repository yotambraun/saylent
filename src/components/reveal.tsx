"use client";
// Moved into @saylent/report (open-source split):
// the report components now live in the package and the same scroll-reveal has to
// ship with them. This file stays as the app's import path so nothing else churns.
export { CountUp, Reveal } from "@saylent/report/components/reveal";
